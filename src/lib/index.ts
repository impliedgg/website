// place files you want to import through the `$lib` alias in this folder.

export type cardData = {
    title?: string,
    description?: string,
    language?: string,
    url?: string
}

export default {};